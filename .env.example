# DA Agent Hub - dbt MCP Configuration Template
# SETUP: Copy this file to .env and replace placeholder values with your actual credentials
# NOTE: .env is in .gitignore and will not be committed

# =============================================================================
# dbt Cloud API Configuration (REQUIRED)
# =============================================================================
# Get your API token: https://cloud.getdbt.com/settings/tokens
# Get your Account ID from dbt Cloud URL: https://cloud.getdbt.com/accounts/<ACCOUNT_ID>

DBT_CLOUD_API_TOKEN=your_dbt_cloud_api_token_here
DBT_CLOUD_ACCOUNT_ID=your_account_id_here

# =============================================================================
# Optional Configuration
# =============================================================================

# dbt Project Configuration (optional - for local dbt development)
# DBT_PROJECT_DIR=/path/to/your/dbt/project
# DBT_PATH=/path/to/dbt/executable

# Production Environment ID (optional - for targeting specific dbt environment)
# DBT_PROD_ENV_ID=your_prod_env_id

# dbt-mcp Feature Flags (optional - uncomment and set to "true" to disable features)
# DISABLE_DBT_CLI=false
# DISABLE_SEMANTIC_LAYER=false
# DISABLE_DISCOVERY=false
# DISABLE_SQL=false
# DISABLE_TOOLS=false
# DISABLE_REMOTE=false
